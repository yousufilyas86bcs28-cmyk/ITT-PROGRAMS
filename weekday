import calendar
n=input("enter the date:");
s=n.split("-");
if len(s)==3:
   day=int(s[0]);
   month=int(s[1]);
   year=int(s[2]);
else:
   print("enter correct date");
dayindex=calendar.weekday(year,month,day);
day_name=calendar.day_name[dayindex];
print(day_name.toupper());
